; KDMS — stderr for Cloud Run / Docker (container logs)
; Search grid can embed many base64 photo/ID blobs in one HTML response (~128M default exhausted).
memory_limit = 512M

display_errors = Off
log_errors = On
error_reporting = E_ALL
error_log = /proc/self/fd/2
