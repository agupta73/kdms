; KDMS — stderr for Cloud Run / Docker (container logs)
; Search/report payloads can embed many base64 photo/ID blobs. Keep under container limit.
memory_limit = 1024M

display_errors = Off
log_errors = On
error_reporting = E_ALL
error_log = /proc/self/fd/2
